import React, { PropTypes } from 'react';

const AvatarSc = (props) => {
  return (
    <div>AvatarSc Content</div>
  );
};

AvatarSc.propTypes = {
  propName: PropTypes.string
};

export default AvatarSc;
