import {css} from 'emotion';

const rowStyle = css`
  display: flex;
  background: #000;
  color: #fff;

`;

const Row = (props) => {
  return (
    <div className={rowStyle}>
     
    </div>
  );
};

export default Row;
